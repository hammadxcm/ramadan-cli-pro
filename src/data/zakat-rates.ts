/**
 * @module data/zakat-rates
 * @description Zakat calculation constants — nisab thresholds and rate.
 */

/** Gold nisab threshold in grams (87.48g = ~7.5 tola). */
export const NISAB_GOLD_GRAMS = 87.48;

/** Silver nisab threshold in grams (612.36g = ~52.5 tola). */
export const NISAB_SILVER_GRAMS = 612.36;

/** Zakat rate as a decimal (2.5%). */
export const ZAKAT_RATE = 0.025;

/** Default gold price per gram in USD (approximate). */
export const DEFAULT_GOLD_PRICE_PER_GRAM = 85;

/** Default silver price per gram in USD (approximate). */
export const DEFAULT_SILVER_PRICE_PER_GRAM = 1.05;
